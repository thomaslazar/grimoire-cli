# IDE
.idea/
.vs/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build outputs
bin/
obj/
out/
build/
dist/
publish/

# Packages
*.nupkg
packages/
node_modules/

# User-specific files
*.user
*.suo
*.log

# Environment
.env
.env.*

# Claude Code (machine-specific settings)
.claude/settings.local.json

# Release artifacts
release-notes.md
release-artifacts/

# Reference material — see CLAUDE.md for how to repopulate
temp
.superpowers/

# Local stack state. /data holds the Grimoire database plus the seeded
# users.json, which Grimoire renames to users.json.imported on first boot.
docker/data/
docker/.env
# ...but the example is meant to be committed; the .env.* rule above would
# otherwise swallow it since gitignore patterns match at any depth.
!docker/.env.example
